import { Directive } from '@angular/core';

/** Marks projected footer actions for `au-card`. Import in the host that projects `[auCardFooter]`. */
@Directive({ selector: '[auCardFooter]' })
export class AuCardFooter {}
