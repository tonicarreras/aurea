import { AuEmptyStateMedia } from './au-empty-state-media.directive';

describe('AuEmptyStateMedia', () => {
  it('is a directive class', () => {
    expect(AuEmptyStateMedia).toBeDefined();
  });
});
