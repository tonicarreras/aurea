import { Directive } from '@angular/core';

/** Marks custom illustration markup (SVG, img, etc.) for {@link AuEmptyState}. */
@Directive({ selector: '[auEmptyStateMedia]' })
export class AuEmptyStateMedia {}
